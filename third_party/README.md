# Third-party sources

Run `scripts/bootstrap_ibex.sh` to clone the official lowRISC Ibex repository into `third_party/ibex`.

The checkout is intentionally ignored by Git. Record the exact upstream commit in evidence manifests. Third-party source remains governed by its own license and notices.
