# v0.8.1 evidence source

This patch release intentionally reuses the immutable live Cerebras evidence committed for `v0.8.0`.

The canonical source is declared in `docs/releases/v0.8.1.evidence-source` and resolves to:

```text
docs/evidence/releases/v0.8.0/cerebras-live
```

No new hosted inference run is claimed. The purpose of `v0.8.1` is to publish the same verified evidence bytes through the new checksum, provenance, byte-download verification, and keyless Sigstore attestation release pipeline.
